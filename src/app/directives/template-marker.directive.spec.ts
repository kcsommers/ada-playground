import { TemplateMarkerDirective } from './template-marker.directive';

describe('TemplateMarkerDirective', () => {
  it('should create an instance', () => {
    const directive = new TemplateMarkerDirective();
    expect(directive).toBeTruthy();
  });
});
